Dear <%= @instructor[:name] %>,
The following are the tentative TA assignments for <%= @position %> <%= @session %>:

Last Name, First Name, Hours, Email
<% @applicants.each do |applicant| %>
<%= applicant[:last_name] %>, <%= applicant[:first_name] %>, <%= applicant[:hours] %>, <%= applicant[:email] %>
<% end %>
